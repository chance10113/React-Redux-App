// import {   } from "../actions/action"

const initialState = {
// img, src? or url? either way = "", and action pastes it in
// fetch ala isFetching
// gotta have an error message
}

export const reducer = (state = initialState, action ) => {
    switch(action.type) {
        default:
            return state
    }
}

export default reducer